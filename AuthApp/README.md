# AuthApp - Мини-приложение с авторизацией и RabbitMQ

Простое веб-приложение для регистрации и авторизации пользователей с отправкой сообщений в RabbitMQ при успешной регистрации.

## 🚀 Запуск

1. Убедитесь, что RabbitMQ запущен:
   ```bash
   docker ps  # Проверьте, что контейнер rabbitmq запущен
   ```

2. Запустите приложение:
   ```bash
   cd AuthApp
   dotnet run
   ```

3. Откройте в браузере: http://localhost:5300

## 📋 Функциональность

- ✅ Регистрация пользователей
- ✅ Авторизация пользователей
- ✅ Отправка сообщений в RabbitMQ при успешной регистрации
- ✅ Простой веб-интерфейс

## 🔧 Настройка

Настройки RabbitMQ находятся в `appsettings.json`:

```json
{
  "RabbitMQ": {
    "HostName": "localhost",
    "Port": "5672",
    "UserName": "guest",
    "Password": "guest",
    "VirtualHost": "/",
    "RegistrationQueueName": "user_registrations"
  }
}
```

## 📡 API Endpoints

### Регистрация
**POST** `/api/auth/register`

```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

### Вход
**POST** `/api/auth/login`

```json
{
  "username": "testuser",
  "password": "password123"
}
```

## 📨 Сообщения в RabbitMQ

При успешной регистрации отправляется сообщение в очередь `user_registrations`:

```json
{
  "userId": 1,
  "username": "testuser",
  "email": "test@example.com",
  "registeredAt": "2024-01-01T12:00:00Z",
  "eventType": "user_registered"
}
```

## 🔗 Интеграция с TasksManager

Ваше основное приложение (TasksManager) может слушать очередь `user_registrations`:

1. Создайте задачу типа "Слушать очередь RabbitMQ"
2. Укажите имя очереди: `user_registrations`
3. Приложение будет получать уведомления о новых регистрациях

## 📝 Примечания

- Пароли хранятся в виде SHA256 хешей (для демонстрации)
- Пользователи хранятся в памяти (при перезапуске данные теряются)
- Для production используйте базу данных и более безопасное хеширование паролей

