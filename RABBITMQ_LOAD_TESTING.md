# 🧪 Тестирование множественных фоновых процессов RabbitMQ

## 📋 Описание

Этот набор скриптов позволяет протестировать сценарии с множеством очередей и множеством фоновых процессов, которые слушают сообщения из RabbitMQ.

## 🎯 Сценарии тестирования

### 1. **test-multiple-consumers.ps1** - Множество очередей, по одному процессу на каждую

**Что тестирует:**
- Создает N очередей (по умолчанию 10)
- Для каждой очереди создает один фоновый процесс-слушатель
- Отправляет сообщения в каждую очередь
- Демонстрирует параллельную обработку разных типов сообщений

**Использование:**
```powershell
.\test-multiple-consumers.ps1
```

**Параметры (можно изменить в скрипте):**
- `$numberOfQueues = 10` - количество очередей
- `$messagesPerQueue = 5` - сообщений на каждую очередь

**Результат:**
- 10 фоновых процессов, каждый слушает свою очередь
- 50 сообщений отправлено (5 на каждую очередь)
- Все процессы работают параллельно

---

### 2. **test-concurrent-consumers.ps1** - Одна очередь, несколько процессов (Load Balancing)

**Что тестирует:**
- Создает одну общую очередь
- Создает несколько воркеров (по умолчанию 5), которые слушают одну и ту же очередь
- Отправляет сообщения в очередь
- Демонстрирует распределение нагрузки между воркерами (round-robin)

**Использование:**
```powershell
.\test-concurrent-consumers.ps1
```

**Параметры:**
- `$sharedQueueName = "shared_work_queue"` - имя общей очереди
- `$numberOfWorkers = 5` - количество воркеров
- `$totalMessages = 20` - всего сообщений

**Результат:**
- 5 воркеров обрабатывают сообщения из одной очереди
- Сообщения распределяются между воркерами
- Демонстрирует горизонтальное масштабирование

---

### 3. **test-complex-scenario.ps1** - Комплексный сценарий

**Что тестирует:**
- Комбинацию всех сценариев:
  1. Одна очередь с несколькими воркерами (load balancing)
  2. Множество очередей, каждая со своим процессом
  3. Высокая нагрузка (много сообщений, много воркеров)

**Использование:**
```powershell
.\test-complex-scenario.ps1
```

**Сценарии:**
1. **Scenario 1:** 3 воркера на очередь `orders_queue`, 15 сообщений
2. **Scenario 2:** 5 очередей (`notifications_1` до `notifications_5`), по 4 сообщения в каждую
3. **Scenario 3:** 4 воркера на очередь `high_volume_queue`, 30 сообщений

**Результат:**
- Множество фоновых процессов работают параллельно
- Разные паттерны обработки сообщений
- Демонстрирует реальные сценарии масштабирования

---

## 🚀 Быстрый старт

### Предварительные требования

1. **RabbitMQ запущен:**
   ```bash
   docker ps  # Проверьте, что контейнер rabbitmq запущен
   ```

2. **Ваше приложение запущено:**
   ```bash
   # Backend должен быть на порту 5270
   cd backend
   dotnet run
   ```

### Запуск тестов

1. **Базовый тест (множество очередей):**
   ```powershell
   .\test-multiple-consumers.ps1
   ```

2. **Тест load balancing (одна очередь, много воркеров):**
   ```powershell
   .\test-concurrent-consumers.ps1
   ```

3. **Комплексный тест (все сценарии):**
   ```powershell
   .\test-complex-scenario.ps1
   ```

---

## 📊 Что проверять после тестов

### 1. Веб-интерфейс приложения

Откройте: http://localhost:5270

**Вкладка "Tasks":**
- Должны быть видны все созданные фоновые процессы
- Статус должен быть "Выполняется" или "Завершена"
- Прогресс показывает количество обработанных сообщений

**Вкладка "Notifications":**
- Должны быть уведомления для каждого полученного сообщения
- Количество уведомлений = количество отправленных сообщений

### 2. RabbitMQ Management UI

Откройте: http://localhost:15672 (guest/guest)

**Queues and Streams:**
- Должны быть видны все созданные очереди
- Кликните на очередь, чтобы увидеть:
  - **Ready:** Сообщения, ожидающие обработки
  - **Unacked:** Сообщения, которые обрабатываются сейчас
  - **Total:** Всего сообщений

**Connections:**
- Должно быть активное подключение от вашего приложения

---

## 🔧 Настройка параметров тестов

Вы можете изменить параметры в каждом скрипте:

### test-multiple-consumers.ps1
```powershell
$numberOfQueues = 10        # Количество очередей
$messagesPerQueue = 5       # Сообщений на очередь
```

### test-concurrent-consumers.ps1
```powershell
$sharedQueueName = "shared_work_queue"
$numberOfWorkers = 5        # Количество воркеров
$totalMessages = 20         # Всего сообщений
```

### test-complex-scenario.ps1
Измените параметры в массиве `$scenarios`:
```powershell
$scenarios = @(
    @{
        QueueName = "orders_queue"
        Workers = 3          # Количество воркеров
        Messages = 15        # Количество сообщений
    },
    # ...
)
```

---

## 💡 Практические сценарии использования

### Сценарий 1: Микросервисная архитектура

**Описание:** Каждый микросервис слушает свою очередь

**Тест:** `test-multiple-consumers.ps1`

**Пример:**
- `orders_queue` → Сервис обработки заказов
- `payments_queue` → Сервис обработки платежей
- `notifications_queue` → Сервис уведомлений
- `inventory_queue` → Сервис управления складом

### Сценарий 2: Горизонтальное масштабирование

**Описание:** Несколько воркеров обрабатывают одну очередь для увеличения пропускной способности

**Тест:** `test-concurrent-consumers.ps1`

**Пример:**
- Очередь `image_processing_queue`
- 10 воркеров обрабатывают загрузку изображений
- При увеличении нагрузки можно добавить больше воркеров

### Сценарий 3: Смешанная архитектура

**Описание:** Комбинация разных паттернов

**Тест:** `test-complex-scenario.ps1`

**Пример:**
- Критичные задачи: одна очередь, несколько воркеров (быстрая обработка)
- Разные типы задач: каждая очередь со своим процессом
- Высокая нагрузка: много воркеров на одну очередь

---

## 🐛 Решение проблем

### Проблема: "API is not available"

**Решение:**
- Убедитесь, что backend запущен на порту 5270
- Проверьте: `netstat -ano | findstr :5270`

### Проблема: Сообщения не обрабатываются

**Решение:**
- Проверьте, что RabbitMQ запущен: `docker ps`
- Проверьте логи приложения на наличие ошибок
- Убедитесь, что задачи созданы и запущены (статус "Выполняется")

### Проблема: Не все сообщения обработаны

**Решение:**
- Это нормально, если тест только что завершился
- Подождите несколько секунд - обработка может продолжаться
- Проверьте RabbitMQ Management - сообщения могут быть в статусе "Unacked"

### Проблема: Слишком много процессов

**Решение:**
- Остановите приложение (Ctrl+C)
- Или завершите задачи через веб-интерфейс (кнопка "Завершить")
- Или измените параметры в скриптах (уменьшите количество)

---

## 📈 Мониторинг производительности

### Метрики для отслеживания:

1. **Количество активных процессов:**
   - Веб-интерфейс → Tasks → Подсчитайте процессы со статусом "Выполняется"

2. **Скорость обработки:**
   - Отслеживайте прогресс задач в реальном времени
   - Сравните время отправки и время обработки

3. **Распределение нагрузки:**
   - В `test-concurrent-consumers.ps1` проверьте, что все воркеры обработали примерно одинаковое количество сообщений

4. **Использование ресурсов:**
   - RabbitMQ Management → Overview → Посмотрите статистику
   - Connections, Channels, Queues

---

## 🎓 Что вы узнаете из тестов

1. **Параллельная обработка:** Как несколько процессов работают одновременно
2. **Load Balancing:** Как RabbitMQ распределяет сообщения между воркерами
3. **Масштабирование:** Как добавить больше воркеров для увеличения пропускной способности
4. **Изоляция:** Как разные очереди изолируют разные типы задач
5. **Мониторинг:** Как отслеживать работу множества процессов

---

## 📚 Дополнительные ресурсы

- **Подробное руководство по интеграции:** `RABBITMQ_INTEGRATION.md`
- **Быстрый старт:** `RABBITMQ_QUICK_START.md`
- **Официальная документация RabbitMQ:** https://www.rabbitmq.com/documentation.html

---

**Готово! Теперь вы можете протестировать любые сценарии с множеством очередей и процессов! 🚀**

